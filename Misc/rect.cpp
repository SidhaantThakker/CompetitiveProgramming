#include <bits/stdc++.h>
#define fi(n) for(int i = 0; i < n; ++i)
using namespace std;
using ll = long long;
using vi = vector<ll>;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int w,h;
    cin>>w>>h;
    cout<<w*h<<'\n';
    return 0;
}